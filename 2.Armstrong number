#include <stdio.h>
int main()
{
	int a,n,rem,res=0;
	printf("enter the value:");
	scanf("%d",&a);
	n=a;
	while(n!=0)
	{
		rem=n%10;
		res+=rem*rem*rem;
		n/=10;
	}
	if(res==a)
	{
		printf("%d is an armstrong number",a);
	}
	else
	{
		printf("%d is not an armstrong number",a); 
	}
	return 0;
}
