#include <stdio.h>
int main()
{
	int fact=1,i,n;
	printf("Enter the element:");
	scanf("%d",&n);
	for(i=1;i<=n;i++)
	{
		fact=fact*i;
	}
	printf("\nThe factorial of a given number is %d",fact);
	return 0;
}
